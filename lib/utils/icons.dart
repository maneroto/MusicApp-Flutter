import 'package:flutter/material.dart';

const delete = Icon(Icons.delete);
const search = Icon(Icons.search);
const close = Icon(Icons.close);
const playing = Icon(Icons.album);
const cancel = Icon(Icons.cancel);
const favorite = Icon(Icons.favorite);
const noFavorite = Icon(Icons.favorite_border);
const viewList = Icon(Icons.view_list);
const filter = Icon(Icons.filter_list);
const menu = Icon(Icons.menu);
const loop = Icon(Icons.loop);
const random = Icon(Icons.swap_horiz);
const play = Icon(Icons.play_arrow);
const pause = Icon(Icons.pause);
const next = Icon(Icons.skip_next);
const previous = Icon(Icons.skip_previous);
